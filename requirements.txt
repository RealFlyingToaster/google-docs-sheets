# Runtime dependencies for the google-docs-sheets plugin.
requests>=2.25
# Required only for the service-account auth path (GOOGLE_SERVICE_ACCOUNT_KEY).
# The OAuth refresh-token / access-token paths work with just `requests`.
google-auth>=2.0
